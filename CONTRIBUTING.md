# 参与开发

## 跑起来

```bash
npm install
npm run dev          # http://localhost:5173
npm test             # 单元测试
npm run test:e2e     # 端到端（需要 dev server 在跑）
```

端到端测试用的是真实的 Chromium 和真实的渲染结果，不是 mock。改完交互请务必跑一遍。

## 这个项目在乎什么

**判断要能被验证。** 工具说"这个接头装不上"，就得有一条能跑的规则说清楚为什么装不上，
而不是凭经验标红。`specCompat.ts`（共同边）、`bracketSeat.ts`（槽线落位）、
`analysis.ts`（干涉）都是这样的规则，每一条都有单元测试。

**先摆对，再拦。** 画错了不阻止你继续画——标出来，你自己决定改不改。
唯一的例外是 2020 顶在 4040 上：没有连接件支持这种搭配，所以连接件不会装上去，
但也不报错。

**没有模式。** 手里拿着型材就是画，空手就是选。不要引入"绘制模式 / 选择模式"这样的开关。

**界面不写文字说明。** 工具栏是图标 + tooltip。要解释的东西写进 README 或 tooltip，
不要挤在按钮上。

## 提交

- 提交信息写**做了什么、为什么**，不要写"根据要求修改"。
- 一个提交做一件事。
- 改了行为就改测试；加了规则就加测试。

## 代码

- TypeScript strict，`npm run build` 必须过。
- 注释解释**为什么**，不解释**是什么**——代码本身说得清的不用再说一遍。
- 新的几何判断放 `src/utils/`，纯函数，不碰 store，这样才测得了。

## 报问题

说清楚：画的是什么、期望什么、实际什么。能附上导出的工程 JSON 或分享链接最好。
